lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier

# CandleScheduler에서 5개의 Job 빈을 @Qualifier로 구분하려고 하는데,
# Lombok의 @RequiredArgsConstructor는 생성자 파라미터에 @Qualifier를
# 전달하지 않습니다. 런타임에 같은 타입의 Job이 5개라 어느 빈을 주입할지 몰라 에러납니다. 라고 해서 추가함